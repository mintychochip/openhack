# Gateway URL for API requests (used at build time for SSR)
# In Docker, this is passed as a build arg
NEXT_PUBLIC_GATEWAY_URL=http://localhost:8000

# API base URL (defaults to same-origin /api proxy)
# NEXT_PUBLIC_API_URL=/api
